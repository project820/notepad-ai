# ANotepad AI rev-4 테스트 체크리스트 (main d7c00de, 2026-07-13) {#lp-h-0}

테스트 대상: 방금 새로 띄운 창. 각 항목 "됨/안됨"만 알려주면 됨. 안되면 번호로. 이번 개정 핵심: **웹 구독 로그인**(형이 rev3에서 제일 빡쳤던 그거), HTML 품질, 잡동사니 정리.

---

## A. 웹 구독 로그인 (제일 중요 — 터미널/API키 없이 ChatGPT처럼) {#lp-h-1}

-    [x]  A1. 설정 → AI Providers 열기
    
-    [x]  A2. Claude 행에 **“구독 로그인”(로그인) 버튼**이 보임 (API 키 입력칸이 대문짝만하게 안 뜸)
    
-   ==\-Claude 👇 이거봐… 예전의 찌꺼기가 그대로 남아있고, Claude 로그인 작동 안됨.==
    
    연결 안 됨무료 로컬 CLI를 사용하려면 터미널에서 \`claude login\`을 실행한 후 앱을 다시 여세요. 또는 아래에 Anthropic API 키를 붙여넣으세요.
    
    무료 로컬 CLI를 사용하려면 터미널에서 \`claude login\`을 실행한 후 앱을 다시 여세요. 또는 아래에 Anthropic API 키를 붙여넣으세요.
    
-    [x]  A3. Claude “구독 로그인” 누름 → 브라우저에 [claude.com](http://claude.com) 로그인 페이지 열림 - A3-a. 브라우저에서 로그인 → 화면에 나오는 **코드를 앱의 입력칸에 붙여넣기** → “로그인됨” 상태로 바뀜  
    \- A2 답변 그대로.
    
-    [x]  A4. Grok 행에 **“구독 로그인” 버튼**이 보임 (API 키 입력칸이 기본으로 안 뜸)
    
-    [x]  A5. Grok “구독 로그인” 누름 → 브라우저에 [accounts.x.ai](http://accounts.x.ai) 페이지 + 코드 자동 인식 - A5-a. 브라우저에서 승인만 하면(붙여넣기 없이) 앱이 자동으로 "로그인됨"으로 바뀜
    
-    [x]  A6. 로그인 후 **로그아웃 버튼**도 보이는지 / 눌러서 로그아웃되는지  
    \-A4~A6 답변  
    Grok 로그인 잘 됨. 연결도 잘 됨.  
    앱 종료 후 새로 실행하면, 로그인 유지가 안되는것 같음.   
    로그아웃 버튼 작동함.
    
-    [x]  A7. 앱 껐다 켜도 로그인 상태 유지되는지 (터미널에서 이미 연동돼 있으면 그대로 잡히는지)  
    로그인유지 안되는것 확인됨.
    

```
🔴 ai provider 세팅 순서 정리 할 필요 있음.1. chat gpt2. claude3. grok 3.1 실행파일 지정<-- 이게 왜 필요한데? 없애버려.---구분선---로컬모델Ollama 만 남겨두기. --> LM studio 삭제.****** 사용자 지정모델 설정 같은건 없애버리고 계정연동만 가능하게 설정하자. 지금의 형태는 좋지않다. Api 키 붙여넣기 기능이랑 실행파일 같은건 삭제. 나중에 부활시킬수 있으나 지금은 의미없다고 봄.
```

## B. 실제 채팅 (로그인 기반으로 돌아가는지) {#lp-h-2}

-    [x]  B1. Claude 모델 골라 채팅 → 응답 옴 (API 키 없이, 구독 로그인만으로)  
     - A2 답변과 마찬가지로 Claude 안되서 못함. (코드가 망가진듯)
-    [x]  B2. Grok 모델 골라 채팅 → 응답 옴 (구독 로그인만으로)  
     - Grok 잘된다!!

## C. API 키는 숨겼는지 (rev3에서 “끔찍하다” 했던 거) {#lp-h-3}

-    [x]  C1. API 키 입력칸이 기본 화면에서 안 보이고 **“고급 설정” 펼치기** 안에만 있는지
-    [x]  C2. (기존에 API 키 넣어 쓰던 사람) 그 키가 사라지지 않고 고급 안에 그대로 남아있는지  
    C1, C2 둘다 없애버려라.  살리고싶으면 ui를 지금보다 훨씬 더 아름답게 재구성 해… 

## D. HTML 생성 품질 (rev3 F1/F2 불만 반영) {#lp-h-4}

-    [x]  D1. 기본 디자인 선택 → HTML 생성 → **버튼류가 너무 작지 않은지** (예전엔 과하게 작았음)
-    [x]  D2. 가로형/세로형 상관없이 **내용이 가운데로만 몰리지 않고 위에서부터 정렬되는지**
-    [x]  D3. 특정 테마(예: BMW 등) 채택 → 색뿐 아니라 **분위기(톤/타이포/여백)** 가 좀 더 반영됐는지 - (여전히 아쉬우면 어떤 점이 아쉬운지 한 줄로)
-   🔴테스트 자체가 실패한다. 그 이유는 지금 데스크탑의 스크린샷 2026-07-13 오후 2.23.45 <-- 이것을 확인해라.  
    HTML 저장버튼 누르면, 기존에 잘 저장되던게 안되고, 대화창 안에서 생성되려 하는 이상한 버그가 발생됨… 뭐냐 이거;

## E. CLI 실행파일 picker (rev3에서 “왜 필요하냐” 했던 거) {#lp-h-5}

-    [x]  E1. “실행파일 직접 지정” 버튼이 기본 화면에서 사라지고 **“고급 설정” 안으로 들어갔는지**
-   **\- 삭제바란다 부탁해!! 제발!! 왜 필요없는걸 자꾸 넣는거야**

## F. 회귀 재확인 (rev3에서 고쳤다던 것들, 이번 빌드에서 다시) {#lp-h-6}

-    [x]  F1. 저장 안 한 새 문서에서 ⌘Q → **3버튼 다이얼로그 뜨고**, Cancel 하면 안 꺼지고 내용 유지 (예전엔 앱이 통째로 강제종료됐음)
-    [x]  F2. 창 2개 다 글 치고 ⌘Q → 각 창 다이얼로그 처리 후 정상 종료
-   \-f1, f2 에서 대용량 파일은 창을 닫을수도 없음. 지속적으로 3버튼 다이얼로그 뜨고, 무엇을 선택하든지 명령대로 하지 않음.  
      
    
-    [x]  F3. 설정에서 언어 변경 → 재시작 안내에서 **Cancel** 누름 → 드롭다운이 **원래 언어로 되돌아옴** (예전엔 안 바뀌었는데 바뀐 척 남아있었음)  
    \- 확인했음, 잘된다.

🔴우선 이 테스트 하는데 있어서 용량이 좀 큰파일을 열었더니 화면에 아무것도 나타나지 않는 버그가 발견됨. 대략 29kb 파일.  
방금 또 69kb 사이즈의 파일을 열었더니 읽는데 엄청나게 오래 걸리는지 대략 10초 정도 이후에 파일이 열림. <-- 최적화 문제 있는듯.  
파일이 커서 로딩이 걸리는 경우, 화면에 로딩 에니메이션 나오게 해라. 고작 69kb 짜리 파일을 읽는데 이렇게 시간이 걸리는건 최고급 노트북에 대한 실례다.

🔴추가 버그 있음.  
체크박스에 tick off 를 하고, 해당 check box 버튼이 아닌, 아랫줄 혹은 근처, 구문점에 커서를 옮기고 마우스 클릭을 하거나 하면, 체크박스가 tick off, on 등이 된다. 실제 커서가 옮겨지지도 않고 방향키로만 줄바꿈 해야됨... 

---
