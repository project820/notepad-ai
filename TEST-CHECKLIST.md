# Notepad AI rev-3 테스트 체크리스트 (main 5760a72, 2026-07-13) {#lp-h-0}

테스트 대상: 방금 새로 띄운 창. 각 항목 "됨/안됨"만 알려주면 됨. 안되면 번호로.

## A. 종료 (제일 중요 — 이게 그 버그) {#lp-h-1}

-    [x]  A1. 아무것도 안 친 새 창에서 ⌘Q → 그냥 바로 꺼짐  
     - 했더니 작업중인 모든 notepad ai 창이 모두 강제종료됨.
-    [x]  A2. 글 몇 자 친 뒤(저장 안 함) 창 닫기(빨간 버튼) → Save/Don’t Save/Cancel 뜸 - \[ \] A2-a. “Don’t Save”(저장 안 함) 누름 → 창이 실제로 닫힘 - \[ \] A2-b. (다시) 글 치고 닫기 → “Save”(저장) 누름 → 저장되고 창 닫힘 - \[ \] A2-c. (다시) 글 치고 닫기 → “Cancel”(취소) 누름 → 창 그대로 유지(안 닫힘=정상)
-    [x]  A3. 창 2개 열고 둘 다 글 친 뒤 ⌘Q → 각 창 다이얼로그 처리 후 앱 전체 종료

## B. 채팅 응답 (증발 버그) {#lp-h-2}

-    [x]  B1. AI 패널에서 “작성” 탭 선택
-    [x]  B2. “의견 좀 줘봐” 같은 메시지 보내기
-    [x]  B3. AI 답변이 **본문 텍스트와 함께** 나옴 (예전처럼 삽입/교체/복사 버튼만 남고 내용 사라지지 않음)

## C. CLI 설정 (Claude / Grok) {#lp-h-3}

-    [x]  C1. 설정 → AI Providers 열기
-    [x]  C2. Claude 행: CLI 상태 표시됨 / 로그인 안내 보임  
     - 이것도, chat GPT 처럼 웹으로 로그인 연동되게 해야지… 지금방식은 터미널에가서 login 한다는게 이 앱이랑 연결이 되는지도 잘 모르겠다. <-- UX 측면에서 끔찍함.
-    [x]  C3. Grok 행: **API 키 입력칸**이 생겼는지 + CLI 상태 배지
-   \- grok api 입력칸 생겼고, 상태배지 생겼는데, 이것도 Chat GPT 와 같은 형태로 유지될 수 있게. 터미널에서 계정연동은 다 되어있음! 
-    [x]  C4. (선택) “실행파일 직접 지정” 버튼으로 파일 선택창 뜨는지
-    - 파일선택창 뜨는데 이게 왜 필요하냐??? 나는 이유를 모르겠음. 

## D. 모델 목록 {#lp-h-4}

-    [x]  D1. 툴바(또는 블록 AI)에서 모델 선택 열기
-    [x]  D2. ChatGPT: GPT-5.6 계열 / 5.5만 보임 (잡동사니 빠짐)
-    [x]  D3. Claude: haiku / sonnet 5 / opus 4.8만
-    [x]  D4. Grok: grok-4.5 보임
-    [x]  D5. OpenRouter: 안 보임 (기존에 OpenRouter 쓰던 게 아니면)
-    [x]  D6. 예전에 고른 모델이 그대로 남아있음(사라지지 않음)

## E. Grok 연동 {#lp-h-5}

-    [x]  E1. 설정에서 Grok API 키 입력 → 저장됨
-    - API 말고 구독모델 베이스로 해야된다고!!!
-    [x]  E2. Grok 모델로 채팅 시도 → 응답 옴 (키 없으면 CLI로, 있으면 API로)
-    - E1이 안되니까 테스트를 못함. claude 도 마찬가지. 웹으로 인증하면 되는 chat GPT 방식은 claude, grok 다 되는데 왜 안되게 해놨는지 모르겟네… 

## F. HTML 생성 {#lp-h-6}

-    [x]  F1. HTML 생성 실행 → 결과물 스타일이 이전보다 나은지 (레이아웃/구성)  
      - [Design.md](http://Design.md) 대신 기본디자인을 선택하고, HTML 생성시, 버튼류가 과하게 작다는 느낌을 받음. 화면 비율 (가로형, 세로형) 선택에 상관없이, 너무 가운데에만 몰리는 형태로 만들어지는 관성이 여전히 있음.  
     - BMW 디자인을 체택한 테스트에서는 컬러는 약간 테마대로 하는것 같지만 여전히 분위기를 재현하려 하는 노력은 안보임… 쓰지못할 정도는 아니지만, 여전히 만족되지 않는다… 라는 의견임. 차라리 깃헙같은데서 좋은 방법을 찾아보는것도 괜찮지 않을까… skill 같은걸 활용해서 하는 방향.

## G. 데이터 보호 물리 스모크 (원래 항목 G011) {#lp-h-7}

-    [x]  G1. 저장 안 한 새 문서에서 ⌘Q → 3버튼 다이얼로그 → Cancel 하면 내용 안 날아감  
    \- 이건 실험을 못함. ⌘Q 누르면 아예 앱 자체가 완전히 강제종료 되어서 안됨. 
-    [x]  G2. 설정에서 언어 변경 → 재시작 요구 시 진행 중 데이터 보호되는지
-   \-테스트 해보겠음. 테스트 성공.  
    \- 버그 발생함. 
-    1. 언어변경 버튼 누름  
     2. 원하는 언어선택
-   3\. 재시작 해야한다는 문구 나오는데 여기서 재시작 버튼을 누르면 당연히 재시작 잘 됨.
-   4\. 그런데, cancel 버튼 누르면, 취소되긴 하지만, 내가 변경하려했던 언어로  선택된 상태로 유지됨 (실제로 변경되지 않았음에도.)
-    [x]  G3. 앱 강제종료(⌘⌥Esc 또는 Activity Monitor) 후 다시 열기 → 마지막 문서 복구되는지
-   오!!!!! 잘된다.

---
